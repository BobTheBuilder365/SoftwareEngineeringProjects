package aa_Interfaces;

public interface IsInSizeRange {
	
	public abstract Boolean isInSizeRange(double lower, double upper);
	/**
	 * mögliche konkretisierung mit size (double):
	 * 
	 */
	// return (this.size >= lower && this.size <= upper ? true : false);
}
